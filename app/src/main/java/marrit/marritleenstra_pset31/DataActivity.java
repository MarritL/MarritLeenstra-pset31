package marrit.marritleenstra_pset31;

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;
import android.widget.ArrayAdapter;
import android.widget.ListView;
import android.widget.TextView;

import org.w3c.dom.Text;

import java.util.ArrayList;

/**
 * Created by Marrit on 2-10-2017.
 */

public class DataActivity extends AppCompatActivity {
    /*TextView mResult;
    ListView mListViewItems;
    ArrayList<Song> trackArray;
    // ArrayList<Sring> trackArray

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_song_list);

        mResult = (TextView) findViewById(R.id.song_artist);
        //lvItems = (ListView) findViewById(R.id.android_list);
        //lvItems = ()

        Bundle extras = getIntent().getExtras();
        trackArray = (ArrayList<Song>) extras.getSerializable("data");

        makeTrackAdapter();
    }

    public void makeTrackAdapter() {
        ArrayAdapter arrayAdapter = new ArrayAdapter<Song>(
                this, android.R.layout.simple_list_item_2, android.R.id.text1,trackArray);

        // initialise listview
        mListViewItems = (ListView)findViewById(R.id.android_list);
        mListViewItems.setAdapter(arrayAdapter);
    }
*/
}
